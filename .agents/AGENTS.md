- The user's name is Anil.
- The user's manager's name is Pradeep.
- Whenever discussing or requiring a new API, ALWAYS provide the endpoint name, method name, input parameters, and output response so it can be handed off to the backend team.

- Never use mock data APIs for the frontend. We are nearing production, so always use the real live APIs.
